// This generic themed switch has no settings-specific logic and now lives in
// `components/ui` (it's imported well outside Settings). Re-exported here so
// existing imports keep working.
export { EmberToggle } from '@/components/ui/forms/ember-toggle';
