.DS_Store
/node_modules/

# React Router
/.react-router/
/build/

/.idea/

# Environment variables
.env
.env.local
.env.development
.env.production

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
.last-run.json

# Claude Code: local-only agent config and guidance. CLAUDE.local.md is the
# personal variant Claude Code reads alongside (or instead of) a committed
# CLAUDE.md, so it stays out of the repo.
.claude/
CLAUDE.local.md